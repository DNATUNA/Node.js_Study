doctype html
html
    head
        meta(charset="utf-8")
        title upload
        link(rel='stylesheet' type='text/css' href='/stylesheets/resetCSS.css')
        link(href='https://fonts.googleapis.com/css?family=Noto+Sans+KR:400,500,700&display=swap&subset=korean' rel='stylesheet')
        link(rel='stylesheet' href='/stylesheets/main_style.css' type='text/css')
        link(rel='stylesheet' href='/stylesheets/upload_style.css' type='text/css')
        script(src='http://code.jquery.com/jquery-3.1.1.js')
        script(src='./javascripts/upload_script.js')
        

    body 
        #wrap
            .headbox
                .inheadbox
                    .logo
                        p #WE ARE HERE
                    .search
                        form    
                            input.searchbox(type='text' name='search' value='')
                            | 
            #bodybox
                .content

                    form(action = "/upload" method = "post" enctype = "multipart/form-data").uploadform
                            .form_line
                                .form_caption 제목
                                input(name = 'title' type = "text").form_title
                            .form_line
                                .form_caption 가격
                                input(name = 'price' type = "text").form_price
                            .form_line
                                .form_caption 내용
                                textarea(name = 'content').form_description
                            .form_line
                                .form_caption 태그
                                input(name = 'tag' type = "text").form_tag
                            input(type = "file" multiple name = "userfile").drop_caption   
                            //- #drop
                            //-     //- 제가 파일 드래그 앤 드롭 부분을 input으로 일단 바꿔놓음.
                                
                            //-     p.drop_caption 파일을 끌어와서 올리기 
                            //-     #thumbnails

                            //- 여기 부분이 원래 button이였는데 서버로 제출하기 위해서 input으로 바꾸고 type값을 submit으로 했습니다.
                            input(type = 'submit' value = 'Upload').upload 
                            | 
                        
